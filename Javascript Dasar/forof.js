const names = ["alim","prasetyo","putra","sinambela"];
for (const name of names) {
    document.writeln(`<p>${name}</p>`)
}
const fullName = "Alim Prasetyo Putra Sinambela";
for (const character of fullName) {
    document.writeln(`<p>${character}</p>`)
}
